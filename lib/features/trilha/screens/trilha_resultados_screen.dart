import 'package:flutter/material.dart';

class TrilhaResultadosScreen extends StatelessWidget {
  const TrilhaResultadosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Parabéns! Trilha Concluída!')),
    );
  }
}
